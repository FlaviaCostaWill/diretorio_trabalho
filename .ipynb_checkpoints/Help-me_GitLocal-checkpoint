atualização / sincronizacao com sagemaker:

cd projetos
cd diretorio_trabalho
git pull origin master



subindoo modificações quando rodo local:

cd projetos
cd diretorio_trabalho
git status
git add .
git status
git commit -m "inclusão / update"
git push origin master


Quando houver conflito no merge

git rm 'HMS/AED HMS.ipynb'
git merge 'HMS/AED HMS.ipynb'
git commit -m 'hms ead'
git push origin master



ERRO:
fatal: Not possible to fast-forward, aborting

git pull --rebase
git push origin master


Zerar as submissions

git reset --soft HEAD~3
